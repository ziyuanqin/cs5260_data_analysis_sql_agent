# CS5260 项目使用说明（多后端版）

## 1. 项目简介

本项目是一个「FastAPI + 原生前端」聊天应用，支持流式返回，并且已经改造成可插拔多后端架构。

当前可以使用两类后端：

- `openai_compatible`：兼容 OpenAI 协议的云端或私有服务
- `local_http`：你们自己的本地 HTTP 聊天后端

---

## 2. 当前目录与职责

- `main.py`：ASGI 启动入口
- `app/factory.py`：创建 FastAPI 应用，注册路由与静态资源
- `app/config.py`：读取环境变量，构建运行配置
- `app/models.py`：接口请求/响应模型
- `app/api/chat.py`：聊天相关接口（stream/reset/providers）
- `app/services/llm_service.py`：会话管理与统一聊天业务逻辑
- `app/services/providers/`：后端适配层（每个后端一个实现）
- `frontend/index.html`：页面结构
- `frontend/app.js`：前端状态管理、发送请求、SSE 解析与渲染
- `frontend/style.css`：样式布局

---

## 3. 框架逻辑（核心调用链）

1. 前端在 `frontend/app.js` 里调用 `POST /api/chat/stream`。
2. `app/api/chat.py` 接收请求并调用 `ChatService.stream_chat_events(...)`。
3. `app/services/llm_service.py` 根据 `provider` 选择具体后端适配器。
4. 适配器在 `app/services/providers/` 中执行真实模型请求，返回文本分片流。
5. `ChatService` 把分片包装为事件（`token/final/error`）。
6. 路由把事件转为 SSE 帧返回前端。
7. 前端实时解析 SSE 并增量渲染。

这套分层的意义是：

- 业务层不关心某家 SDK 细节
- 新增后端时只改 `providers/` 和注册表
- 前后端接口保持稳定

---

## 4. 运行方式

建议环境：

- Python 3.10+
- pip

安装依赖（在项目根目录 `CS5260/` 下）：

```bash
pip install fastapi uvicorn pydantic openai
```

启动服务：

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8001
```

浏览器访问：

- `http://127.0.0.1:8001`

---

## 5. 配置项说明（环境变量）

项目现在支持配置文件，建议优先使用根目录 `app_config.json`。

配置优先级：

1. `app_config.json`
2. 环境变量
3. 代码默认值

`app_config.json` 示例：

```json
{
  "default_provider": "openai_compatible",
  "openai_api_base_url": "https://yunwu.ai/v1",
  "openai_api_key": "",
  "openai_model_name": "gpt-5.2",
  "local_api_url": "http://127.0.0.1:9000/chat",
  "local_model_name": "local-default-model",
  "request_timeout": 100,
  "history_max_messages": 20
}
```

### 5.1 通用配置

- `LLM_DEFAULT_PROVIDER`：默认后端，默认 `openai_compatible`
- `YUNWU_TIMEOUT`：请求超时秒数，默认 `100`
- `YUNWU_HISTORY_MAX_MESSAGES`：会话保留消息数，默认 `20`

### 5.2 OpenAI 兼容后端

- `OPENAI_COMPAT_BASE_URL`：兼容接口地址
- `OPENAI_COMPAT_MODEL`：默认模型名
- `openai_api_key`：请在 `app_config.json` 中配置密钥

兼容旧变量：

- `YUNWU_BASE_URL`、`YUNWU_MODEL` 仍可作为兜底

### 5.3 本地 HTTP 后端

- `LOCAL_LLM_API_URL`：本地接口地址，默认 `http://127.0.0.1:9000/chat`
- `LOCAL_LLM_MODEL`：本地默认模型名

---

## 6. API 说明

### 6.1 流式聊天

- 路径：`POST /api/chat/stream`
- 请求体：

```json
{
  "session_id": "string",
  "mode": "general | analyst",
  "message": "用户输入",
  "provider": "openai_compatible | local_http",
  "model": "可选，覆盖默认模型",
  "provider_options": {},
  "stream": true
}
```

SSE 事件：

- `event=token`：增量文本，字段 `payload.delta`
- `event=final`：完整文本，字段 `payload.text`
- `event=error`：错误信息，字段 `payload.message`

### 6.2 清理会话

- 路径：`POST /api/chat/reset`

### 6.3 查看可用后端

- 路径：`GET /api/chat/providers`
- 返回示例：

```json
{
  "providers": ["local_http", "openai_compatible"]
}
```

---

## 7. 前端切换后端方式

前端默认不传 provider，会走后端默认配置。

如果你想临时切换，可在浏览器控制台执行：

```js
window.__CHAT_BACKEND_CONFIG__ = {
  provider: "local_http",
  model: "qwen2.5:7b",
  provider_options: {}
};
```

刷新页面后生效。

---

## 8. 如何新增一个后端调用（重点）

目标：接入第 3 家后端时，不改主流程，只新增适配器。

### 步骤 1：新建 provider 文件

在 `app/services/providers/` 下新增，例如 `my_backend_provider.py`。

最小模板：

```python
from collections.abc import Generator


class MyBackendProvider:
    def __init__(self, api_url: str, api_key: str | None = None):
        self.api_url = api_url
        self.api_key = api_key

    def stream_chat(
        self,
        messages: list[dict[str, str]],
        model: str,
        timeout: int,
        provider_options: dict | None = None,
    ) -> Generator[str, None, None]:
        # 这里调用你们的后端接口
        # 最终只需要 yield 文本分片（str）即可
        yield "示例分片"
```

### 步骤 2：在注册表注册

编辑 `app/services/providers/registry.py`，加入：

```python
"my_backend": MyBackendProvider(...)
```

### 步骤 3：补配置

如需环境变量，在 `app/config.py` 的 `AppConfig` 和 `load_config(...)` 中增加字段。

### 步骤 4：验证

1. 调 `GET /api/chat/providers`，确认出现 `my_backend`。
2. 请求 `POST /api/chat/stream` 时传 `provider="my_backend"`。
3. 前端确认可流式出字。

---

## 9. 本地后端接口建议规范

为了减少适配成本，建议你们本地后端满足以下约定：

- 输入：`model/messages/stream/provider_options`
- 输出支持任一形式：
- `data: {...}`（SSE）
- 每行一个 JSON（如 `{ "delta": "..." }`）
- 纯文本分片

当前 `local_http_provider.py` 已兼容以上三种常见格式。

---

## 10. 常见问题

### Q1：报错“Could not import module main”

- 确认在 `CS5260/` 目录启动。
- 或使用 `--app-dir` 指定到 `CS5260/`。

### Q2：前端返回“请求失败”

- 检查后端日志是否有 `event=error`。
- 检查 provider 是否存在（可调 `/api/chat/providers`）。
- 如果是 `openai_compatible`，确认密钥与 base_url 正确。

### Q3：切到 `local_http` 没返回

- 检查 `LOCAL_LLM_API_URL` 是否可访问。
- 先用 Postman/curl 验证本地接口是否确实能流式返回。

---

## 11. 你们后续最推荐的扩展路径

1. 先稳定一个本地后端协议。
2. 每接一类后端就新增一个 provider，不要改 `ChatService` 主逻辑。
3. 如果 provider 变多，再把注册表改成自动加载（插件式）。
