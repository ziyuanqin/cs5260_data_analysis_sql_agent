# 角色 C 前端实现：使用说明

## 1. 项目目的

本目录提供了一个可直接运行的 Demo，用于完成角色 C（前端与交互）核心任务：

- 双模式切换（`general` / `analyst`）
- `Streaming` 流式展示（SSE）
- 多模态渲染（文本、表格、图表、文件）
- 基础交互控制（`Stop`、`Retry`、`Regenerate`）

---

## 2. 目录结构

- `main.py`：FastAPI 后端（SSE 接口 + 静态页面托管）
- `frontend/index.html`：页面结构
- `frontend/style.css`：样式与布局
- `frontend/app.js`：核心前端逻辑（状态管理、SSE 解析、消息渲染）

---

## 3. 运行环境

建议：

- Python 3.10+
- pip
- 浏览器（Chrome / Edge / Safari）

安装依赖：

```bash
cd /Users/lzy/Documents/nus/cs5260/lab
pip install fastapi uvicorn pydantic
```

启动服务：

```bash
uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

打开页面：

- http://127.0.0.1:8000

---

## 4. 功能验证（建议按顺序）

### 4.1 双模式

1. 先用“全能模式”发送一句话。  
2. 再切到“分析师模式”发送同样的话。  
3. 观察两种模式返回内容差异。

### 4.2 Streaming

1. 发送消息后，观察 AI 回复是否“逐字出现”。  
2. 点击 `Stop`，确认输出可中断。  
3. 点击 `Retry` / `Regenerate`，确认可重新生成。

### 4.3 多模态渲染

在分析师模式下，确认页面出现：

- 文本说明
- 表格
- 图表（示例占位图）
- 文件下载卡片

---

## 5. 前后端接口约定（当前 Demo）

请求路径：`POST /api/chat/stream`

请求体：

```json
{
  "session_id": "string",
  "mode": "general | analyst",
  "message": "用户输入",
  "stream": true
}
```

SSE 事件格式（`data: {...}`）：

- `event=token`：增量文本（`payload.delta`）
- `event=tool_result`：工具输出（`content_type` 可为 `table/chart/file/text`）
- `event=final`：完成事件
- `event=error`：错误事件

---

## 6. 常见问题

### Q1：页面打开失败

- 确认 `uvicorn` 正在运行。
- 确认端口是 `8000`。
- 确认访问的是 http://127.0.0.1:8000。

### Q2：IDE 报 `fastapi` / `pydantic` 未找到

- 说明当前解释器没装依赖。
- 在当前 Python 环境重新执行安装命令。

### Q3：前端提示流式请求失败

- 检查 API Base URL 是否是 `http://127.0.0.1:8000`。
- 检查后端是否有启动日志和报错。

---

## 7. 下一步接入真实项目

当你们 A/B 角色后端准备好后，建议按这三步接入：

1. 保持 `event` 字段稳定（`token/tool_result/final/error`）。
2. 固化 `content_type` 与 `payload` JSON Schema。
3. 先替换 `main.py` 的 mock 逻辑，再逐项联调前端渲染。

---

## 8. 学习建议（针对这份代码）

### 阶段 1：先跑通（半天）

目标：知道每个文件负责什么。  
行动：启动服务，点一遍所有按钮，看一次完整交互流程。

### 阶段 2：读主流程（1 天）

重点阅读顺序：

1. `frontend/app.js` 中 `streamChat()`（发请求 + 读 SSE）
2. `handleServerEvent()`（事件分发）
3. `renderPart()`（多模态渲染入口）
4. `main.py` 中 `chat_stream()`（后端如何产出事件）

学习方法：边读边在关键函数打 `console.log` / `print`。

### 阶段 3：做小改动（1~2 天）

建议练习题：

- 给模式切换增加 tooltip。
- 给表格增加“复制 CSV”按钮。
- 新增 `content_type=html` 的渲染器。
- 给 `error` 事件加“重连”按钮。

原则：每次只改一个点，改完就自测。

### 阶段 4：理解工程化（持续）

- 把“状态”与“渲染”分层（便于迁移 React/Vue）。
- 将事件协议整理成独立文档。
- 补单元测试（尤其是事件解析与渲染分发）。

---

## 9. 推荐学习节奏

- 第 1 周：只关注“能跑通 + 能解释主流程”。
- 第 2 周：做 3 个小功能改动。
- 第 3 周：接入真实后端协议并完成联调。
- 第 4 周：整理文档与演示脚本。

如果你能做到“看懂、能改、能讲清楚”，这份代码就算学会了。
