### **1\. 团队分工（Role-Based Assignment）**

根据 Agent 的架构逻辑，建议将 4 个人划分为如下角色：

#### **角色 A：架构与中枢（Lead Engineer/Architect）**

* **职责**：负责 LangGraph 的整体拓扑结构（节点与边的逻辑）、状态管理（State Definition）、以及多模型分发的逻辑。  
* **核心任务**：  
  * 搭建基础的 FastAPI 框架。  
  * 编写全能模式下的 **Router（路由）** 逻辑，确保模型能根据意图切换。  
  * 负责各模块的“接口契约”，确保其他人的代码能顺利并入 Graph。

#### **角色 B：分析专家与工具链（Tool/Data Specialist）**

* **职责**：深挖“数据分析专家模式”。  
* **核心任务**：  
  * 开发 Python 代码执行沙盒（安全执行环境）。  
  * 优化数据分析的 **System Prompt**。  
  * 实现高级功能：如自动读取 CSV/Excel、调用 Matplotlib 生成统计图表并返回给前端展示。

#### **角色 C：前端与交互（UX/UI Developer）**

* **职责**：负责用户界面、模式切换按钮、以及复杂的交互状态。  
* **核心任务**：  
  * 设计双模式切换的 UI 逻辑。  
  * 实现 **Streaming（流式传输）** 显示（让用户看到 AI 思考的过程）。  
  * 处理多模态展示：当分析师模式返回图表或表格时，前端能优雅地渲染。

#### **角色 D：质量与工程化（DevOps/QA Engineer）**

* **职责**：负责项目部署、Prompt 调优、测试与文档。  
* **核心任务**：  
  * 配置 Railway/Vercel 的 CI/CD 自动部署。  
  * 评估模型表现（对比 GPT-4o 和其他 API 在不同场景下的优劣）。  
  * 编写项目的 README 和演示文档，录制 Demo 视频。

---

### **2\. 三个月的工作流程（Milestones）**

#### **第一个月：底座搭建（MVP 阶段）**

* **第 1-2 周**：技术选型对齐。A 搭建出 LangGraph 的基本雏形，B 写出最简单的代码执行函数。  
* **第 3-4 周**：前后端调通。实现最基础的按钮切换：点击 A 按钮模型变全能，点击 B 按钮模型变专家。

#### **第二个月：深度优化（功能爆发期）**

* **第 5-6 周**：**B** 优化分析师模式。支持处理真实数据集，而不仅是模拟结果。**A** 接入更多模型（如 DeepSeek/Claude）实现全能模式的“多模型融合”。  
* **第 7-8 周**：**C** 优化前端，支持图表展示、Markdown 渲染。**D** 进行 Prompt 压力测试，减少分析师模式下的代码报错率。

#### **第三个月：工程化与打磨（冲刺阶段）**

* **第 9-10 周**：加入“错误自愈”逻辑（LangGraph 的循环纠错）。如果代码运行报错，Agent 会自动重试。  
* **第 11 周**：全员进行 Bug Fix。**D** 负责将项目部署到公网，确保所有人都能流畅访问。  
* **第 12 周**：整理代码注释，编写高质量 README，准备小组汇报。